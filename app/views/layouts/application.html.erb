<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <!-- Connect Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">

    <!-- Connect Bootstrap JavaScript and its dependencies -->
    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-gtEjrD/SeCtmISkJkNUaaKMoLD0//ElJ19smozuHV6z3Iehds+3Ulb9Bn9Plx0x4" crossorigin="anonymous"></script>

    <!-- Connect Font Awesome -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/js/all.min.js"></script>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    
    <style>
     {
       border: thin red dashed
     }

     .logo{
       color: rgb(239, 131, 84);
     }

    .btn-circle {
      width: 30px;
      height: 30px;
      padding: 6px 0px;
      border-radius: 15px;
      font-size: 8px; 
      text-align: center;
      color: none;
      border-color: none;
      }

    .home{
     border-radius: 20px;
     background-color: rgb(247, 247, 248);
     border: none;
    }

    .material{
      border-radius: 10px;
    }

    .overlay {
      position: absolute;
      top: 0;
      bottom: 0;
      left: 0;
      right: 0;
      height: 100%;
      width: 100%;
      opacity: 0;
      transition: .5s ease;
      background-color: rgb(245, 217, 204);
      border-radius: 20px;
    }

    .text {
      color: rgb(239, 131, 84);
      position: absolute;
      top: 50%;
      left: 50%;
      -webkit-transform: translate(-50%, -50%);
      -ms-transform: translate(-50%, -50%);
      transform: translate(-50%, -50%);
      text-align: center;
    }

    .test:hover .overlay{
      background-color: rgb(245, 217, 204);
      transition: .4s;
      opacity: 1;
     }

    .overpalette {
      position: absolute;
      top: 0;
      bottom: 0;
      left: 0;
      right: 0;
      height: 100%;
      width: 100%;
      opacity: .5;
      transition: .5s ease;
      background-color: rgb(219, 255, 205);
      border-radius: 10px;
    }

    .palette:hover{
      background-color: 
      background-color: rgb(219, 255, 205);
      transition: .4s;
      opacity: .5;
    }

    .signin-btn {
      border-radius: 10px;
      text-align: center;
    }

    .signin-btn:hover {
      border-radius: 10px;
      background-color: rgb(219, 255, 205);
    }

    </style>

  </head>

  <body>

  <% if request.path != "/" %>

  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand"><strong>Suppl</strong></a>
   
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/materials"><button class="btn btn-outline-dark btn-sm">Materials <i class="fas fa-tree"></i></button></a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/palettes"><button class="btn btn-outline-dark btn-sm">Palettes <i class="fas fa-swatchbook"></i></button></a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/projects"><button class="btn btn-outline-dark btn-sm">Projects <i class="fas fa-home"></i></button></a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/user/<%=session.fetch(:user_id)%>"><button class="btn btn-outline-dark btn-sm">Dashboard <i class="fas fa-tachometer-alt"></i></button></a>
          </li>
        </ul>

        <div class="dropdown">
          <button class="btn" type="button" id="dropdownMenuButton2" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="far fa-user-circle fa-2x"></i>
          </button>
    
            <ul class="dropdown-menu class dropdown-menu-lg-end" aria-labelledby="dropdownMenuButton2">
              <% if session.fetch(:user_id) != nil %>
              <li><div class="text-center"><strong>@<%= @current_user.username %></strong></div></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="/user/<%=session.fetch(:user_id)%>">Dashboard</a></li>
              <li><a class="dropdown-item" href="/user_sign_out">Sign out</a></li>
              <% end %>
            </ul>
        </div>

        <%= render "/user_authentication/edit_profile_modal" %>

      </div>
    </div>
  </nav>
<% end %>

<div class="container mt-4">


  <% if notice != nil %>
  <div class="row">
    <div class="col-md-4">
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= notice %>
        <button type="button" class="btn-close btn-sm" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    </div>
  </div>


  <% end %>

  <% if alert != nil %>
    <div class="alert alert-danger" role="alert"><%= alert %></div>
  <% end %>

<%= yield %>

</div>

  </body>
</html>