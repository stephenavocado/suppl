<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <!-- Connect Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">

    <!-- Connect Bootstrap JavaScript and its dependencies -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.4.1/jquery.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.bundle.min.js"></script>

    <!-- Connect Font Awesome -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/js/all.min.js"></script>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Suppl</a>
   
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto mb-2 mb-lg-0">
      </ul>
      <% if request.path != "/" %>
      <a class="btn btn-outline-info" href="/"><i class="fas fa-igloo"></i></a>
      <% end %>

      <% if session.fetch(:user_id) == nil %>
      <a class="btn btn-success" href="/user_sign_in">Sign In</a>
      <a class="btn btn-warning" href="/user_sign_up">Sign Up</a>
      <% end %>

      <% if session.fetch(:user_id) != nil %>
      <a class="btn btn-danger" href="/user_sign_out">Sign Out</a>
      <a class="btn btn-outline-dark" href="/projects"><i class="fas fa-building"></i></a>
      <a class="btn btn-outline-dark" href="/palettes"><i class="fas fa-swatchbook"></i></a>
      <% end %>
      
      <% if request.path != "/" %>
      <form class="d-flex">
        <input class="form-control me-2" type="search" name="query_search" placeholder="Suppl away..." aria-label="Search">
        <button class="btn btn-outline-dark" type="submit"><i class="fas fa-search"></i></button>
      </form>
      <% end %>
    </div>
  </div>
</nav>

<% if notice != nil %>
<div class="alert alert-success" role="alert"><%= notice %></div>
<% end %>

<% if alert != nil %>
<div class="alert alert-danger" role="alert"><%= alert %></div>
<% end %>

    <%= yield %>
  </body>
</html>
